CREATE TABLE PRODUTO(
	CODIGO INT PRIMARY KEY,
	NOME VARCHAR (100),
	PRECO DECIMAL (9,2),
	PESO INT (9,2)
)

INSERT INTO PRODUTO (CODIGO, NOME, PRECO, PESO)
VALUES (1, 'martelo', 20.00, 5);
INSERT INTO PRODUTO (CODIGO, NOME, PRECO, PESO)
VALUES (2, 'furadeira', 35.00, 6);
INSERT INTO PRODUTO (CODIGO, NOME, PRECO, PESO)
VALUES (3, 'chave inglesa', 15.00, 2);
INSERT INTO PRODUTO (CODIGO, NOME, PRECO, PESO)
VALUES (4, 'alicate', 14.00, 2);
INSERT INTO PRODUTO (CODIGO, NOME, PRECO, PESO)
VALUES (5, 'pregos', 15.00, 4);

CREATE TABLE CLIENTE(
	IDCLIENTE INT PRIMARY KEY,
	NOME VARCHAR(100),
	CIDADE VARCHAR(100) NOT NULL
)

INSERT INTO CLIENTE (IDCLIENTE, NOME, CIDADE)
VALUES (1, 'João', 'Rio de Janeiro’);
INSERT INTO CLIENTE (IDCLIENTE, NOME, CIDADE)
VALUES (2, 'Pedro', 'São Paulo');
INSERT INTO CLIENTE (IDCLIENTE, NOME, CIDADE)
VALUES (3, 'Maria', 'Rio Grande do Sul');
INSERT INTO CLIENTE (IDCLIENTE, NOME, CIDADE)
VALUES (4, 'Roberto', 'Jaguaruna');
INSERT INTO CLIENTE (IDCLIENTE, NOME, CIDADE)
VALUES (5, 'Cleiton', 'Florianópolis');

ALTER TABLE CLIENTE
ADD EMAIL VARCHAR(150);

ALTER TABLE PRODUTO
ADD ESTOQUE INT;

UPDATE CLIENTE
SET CIDADE = 'Curitiba'
WHERE IDCLIENTE = 1;

UPDATE PRODUTO
SET PRECO = PRECO * 1.10
WHERE PESO > 10;

DELETE FROM PRODUTO
WHERE CODIGO = 1;

DELETE FROM CLIENTE
WHERE CIDADE = 'Jaguaruna';

SELECT * FROM PRODUTO
ORDER BY PRECO;

SELECT * FROM CLIENTE
ORDER BY NOME DESC;

SELECT SUM(PRECO) AS TOTAL_PRECOS
FROM PRODUTO;

SELECT CIDADE, COUNT(*) AS TOTAL_CLIENTES
FROM CLIENTE
GROUP BY CIDADE;
